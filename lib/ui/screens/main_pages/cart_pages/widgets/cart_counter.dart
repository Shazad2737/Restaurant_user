import 'package:flutter/material.dart';

class cart_counter extends StatelessWidget {
  const cart_counter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("gd"),
    );
  }
}
